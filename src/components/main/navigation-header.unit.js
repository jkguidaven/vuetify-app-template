import NavigationHeader from './navigation-header.vue'

describe('@components/navigation-header', () => {
  it('exports a valid component', () => {
    expect(NavigationHeader).toBeAComponent()
  })
})
